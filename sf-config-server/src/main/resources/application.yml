spring:  application:    name: sf-config-server  profiles:    active: dev,jdbc  cloud:    config:      server:        jdbc:          sql: SELECT `key`, `value` from lx_config where APPLICATION=? and PROFILE=? and LABEL=?server:  port: 8888eureka:  instance:    prefer-ip-address: truemanagement:  endpoints:    web:      exposure:        include: "*"  endpoint:    health:      show-details: ALWAYS